'use client';

export default function ProfileModal() {
  return (
    <div
      className="modal fade"
      id="enSavoirPlusModal"
      tabIndex={-1}
      aria-hidden="true"
    >
      <div className="modal-dialog modal-dialog-centered">
        <div className="modal-content">{/* vide pour l’instant */}</div>
      </div>
    </div>
  );
}
